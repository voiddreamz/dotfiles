#!/usr/bin/sh
# @author nate zhou
# @since 2025
# parse shell aliases `fgl` for previewing with git in fzf

commit_HASH=$(echo "$@" | sed 's/^[^a-Z0-9]*//' | cut -d' ' -f1)

git show $commit_HASH | bat --style=changes
