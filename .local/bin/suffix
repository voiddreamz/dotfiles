#!/usr/bin/sh
# @author nate zhou
# @since 2025
# Also see `./prefix`

usage() {
    cat << _EOF_
Usage:  suffix FILE|String
        get a file's extension name
_EOF_
exit 0
}

[ -z "$1" ] && usage
echo ${1##*.}
